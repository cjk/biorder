<h3>Your history:</h3>

<ul>
  {{#each order in controller}}
    <li>
      <p class="order">Order from [{{order.createdAt}}] with {{order.lineitems.length}} lineitems {{#link-to 'orders.edit' order}}<i class="md md-edit"></i>{{/link-to}}</p>
    </li>
  {{/each}}
</ul>
